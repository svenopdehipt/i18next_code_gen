name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  i18next: ^0.7.0
  i18next_code_gen:
    path: ../

dev_dependencies:
  build_runner: ^2.0.0
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - lib/i18n/en-US/